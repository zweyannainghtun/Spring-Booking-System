package com.digitalbase.ticketing.model;import jakarta.persistence.*;import lombok.*;import java.time.Instant;@Entity@Table(name = "client_products")@Data@Builder@NoArgsConstructor@AllArgsConstructorpublic class ClientProduct {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "user_id", nullable = false)    private User user;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "product_id", nullable = false)    private Product product;    @Column(name = "support_plan")    private String supportPlan;    @Column(name = "purchased_date", nullable = false)    private Instant purchasedDate;    @Column(name = "plan_end_date", nullable = false)    private Instant planEndDate;    @Column(name = "available_minutes")    private Integer availableMinutes; // ဥပမာ - နာရီ ၁၀၀ ဆိုလျှင် ၆၀၀၀ ဟု သိမ်းမည်    @Column(name = "total_minutes")    private Integer totalMinutes;    @Column(name = "is_active")    private boolean isActive;}