package com.digitalbase.ticketing.model;import com.digitalbase.ticketing.enums.ProductStatus;import jakarta.persistence.*;import lombok.*;import org.hibernate.annotations.CreationTimestamp;import org.hibernate.annotations.UpdateTimestamp;import java.time.Instant;@Entity@Table(name = "products")@Data@Builder@NoArgsConstructor@AllArgsConstructorpublic class Product {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    @Column(nullable = false)    private String name; // ဥပမာ - Mari’s Nail Salon    @Column(columnDefinition = "TEXT")    private String description; // ဥပမာ - Service Booking and Management System    @Column(name = "product_type")    private String productType; // ဥပမာ - Salon Management System    @Column(name = "image_url")    private String imageUrl; // ပုံပြသရန်    @Enumerated(EnumType.STRING)    @Column(nullable = false)    private ProductStatus status; // ACTIVE, INACTIVE    @CreationTimestamp    @Column(name = "created_at", updatable = false)    private Instant createdAt;    @UpdateTimestamp    @Column(name = "updated_at")    private Instant updatedAt;}