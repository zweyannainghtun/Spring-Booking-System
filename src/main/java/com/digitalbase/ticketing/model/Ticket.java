package com.digitalbase.ticketing.model;import com.digitalbase.ticketing.enums.IssueType;import com.digitalbase.ticketing.enums.Priority;import com.digitalbase.ticketing.enums.Team;import com.digitalbase.ticketing.enums.TicketStatus;import com.fasterxml.jackson.annotation.JsonIgnore;import jakarta.persistence.*;import lombok.*;import org.hibernate.annotations.CreationTimestamp;import org.hibernate.annotations.JdbcTypeCode;import org.hibernate.annotations.UpdateTimestamp;import org.hibernate.type.SqlTypes;import java.time.Instant;import java.util.ArrayList;import java.util.List;@Entity@Table(name = "tickets")@Getter@Setter@NoArgsConstructor@AllArgsConstructor@Builderpublic class Ticket {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    // Customer    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "customer_id", nullable = false)    @JsonIgnore    private User customer;    // ယခင် Product အစား ClientProduct (ဝယ်ယူထားသော ပက်ကေ့ချ်) သို့ ပြောင်းချိတ်ခြင်း    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "client_product_id", nullable = false)    private ClientProduct clientProduct;    // UI မှ Issue Type    @Enumerated(EnumType.STRING)    @Column(name = "issue_type", nullable = false)    private IssueType issueType;    // UI မှ Issue Summary (ယခင် serviceName အစား အသုံးပြုရန်)    @Column(name = "summary", nullable = false)    private String summary;    @Lob    @JdbcTypeCode(SqlTypes.LONGVARCHAR)    @Column(nullable = false)    private String description;    // ဖိုင်တွဲတင်ရန် (Drag and Drop Attachments)    @OneToMany(mappedBy = "ticket", cascade = CascadeType.ALL, orphanRemoval = true)    @JsonIgnore    private List<TicketAttachment> attachments = new ArrayList<>();    // ဤ Ticket အတွက် အသုံးပြုလိုက်သော နာရီ (ClientProduct မှ ပြန်နှုတ်ရန်)    @Column(name = "spent_minutes")    private Integer spentMinutes;    // --- အောက်ပါတို့မှာ သင်၏ ယခင် Table အဟောင်းမှ ကောင်းမွန်သော Field များဖြစ်သည် ---    @org.hibernate.annotations.ColumnDefault("1")    @Column(name = "stage")    private Integer stage = 1;    @Enumerated(EnumType.STRING)    @Column(nullable = false)    private TicketStatus status = TicketStatus.NEW;    @Enumerated(EnumType.STRING)    private Priority priority;    @Enumerated(EnumType.STRING)    @Column(name = "assigned_team")    private Team assignedTeam;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "admin_id")    private User admin;    @ManyToOne(fetch = FetchType.LAZY)    @JoinColumn(name = "developer_id")    private User developer;    @CreationTimestamp    @Column(name = "created_at", updatable = false)    private Instant createdAt;    @UpdateTimestamp    @Column(name = "updated_at")    private Instant updatedAt;}