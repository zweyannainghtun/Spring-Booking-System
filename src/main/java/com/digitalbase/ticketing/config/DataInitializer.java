package com.digitalbase.ticketing.config;import com.digitalbase.ticketing.enums.Role;import com.digitalbase.ticketing.model.User;import com.digitalbase.ticketing.repository.UserRepository;import lombok.RequiredArgsConstructor;import org.slf4j.Logger;import org.slf4j.LoggerFactory;import org.springframework.boot.CommandLineRunner;import org.springframework.security.crypto.password.PasswordEncoder;import org.springframework.stereotype.Component;import org.springframework.transaction.annotation.Transactional;@Component@RequiredArgsConstructorpublic class DataInitializer implements CommandLineRunner {    private static final Logger log = LoggerFactory.getLogger(DataInitializer.class);    private final UserRepository userRepository;    private final PasswordEncoder passwordEncoder;    @Override    @Transactional    public void run(String... args) throws Exception {        String adminEmail = "zweyanaingtun@gmail.com";        // Admin Email ဖြင့် Account ရှိ/မရှိ စစ်ဆေးခြင်း        if (!userRepository.existsByEmail(adminEmail)) {            User admin = User.builder()                    .email(adminEmail)                    .code("ADMIN-001")                    // Entity မှာ passwordHash လို့ ပေးခဲ့တဲ့အတွက် builder မှာ passwordHash လို့ သုံးရပါမယ်                    .passwordHash(passwordEncoder.encode("Admin@123"))                    .role(Role.ADMIN)                    .build();            userRepository.save(admin);            log.info("✅ Admin Account created successfully! Email: {} Password: {}", adminEmail, "Admin@123");        } else {            log.info("ℹ️ Admin account already exists.");        }    }}