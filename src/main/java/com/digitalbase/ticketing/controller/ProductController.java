package com.digitalbase.ticketing.controller;import com.digitalbase.ticketing.dto.ProductRequest;import com.digitalbase.ticketing.dto.ProductResponse;import com.digitalbase.ticketing.service.ProductService;import lombok.RequiredArgsConstructor;import org.springframework.http.HttpStatus;import org.springframework.http.ResponseEntity;import org.springframework.security.access.prepost.PreAuthorize;import org.springframework.web.bind.annotation.*;import java.util.List;import java.util.Map;@RestController@RequestMapping("/api/admin/products") // (အကြံပြုချက် - နောက်ပိုင်းတွင် /api/products ဟု ပြောင်းလဲရန် စဉ်းစားနိုင်ပါသည်)@RequiredArgsConstructor// Class Level တွင်ရှိသော @PreAuthorize ကို ဖယ်ရှားလိုက်ပါပြီpublic class ProductController {    private final ProductService productService;    // ၁။ Create ကို Admin သီးသန့် ကန့်သတ်ခြင်း    @PreAuthorize("hasRole('ADMIN')")    @PostMapping(consumes = {"multipart/form-data"})    public ResponseEntity<ProductResponse> createProduct(@ModelAttribute ProductRequest request) {        return new ResponseEntity<>(productService.createProduct(request), HttpStatus.CREATED);    }    // ၂။ Product များကို ကြည့်ရှုခြင်း (Login ဝင်ထားသော မည်သည့် Role မဆို ကြည့်ခွင့်ပြုမည်)    @PreAuthorize("isAuthenticated()")    @GetMapping    public ResponseEntity<List<ProductResponse>> getAllProducts() {        return ResponseEntity.ok(productService.getAllProducts());    }    @PreAuthorize("isAuthenticated()")    @GetMapping("/{id}")    public ResponseEntity<ProductResponse> getProductById(@PathVariable Long id) {        return ResponseEntity.ok(productService.getProductById(id));    }    // ၃။ Update ကို Admin သီးသန့် ကန့်သတ်ခြင်း    @PreAuthorize("hasRole('ADMIN')")    @PutMapping(value = "/{id}", consumes = {"multipart/form-data"})    public ResponseEntity<ProductResponse> updateProduct(@PathVariable Long id, @ModelAttribute ProductRequest request) {        return ResponseEntity.ok(productService.updateProduct(id, request));    }    // ၄။ Delete ကိုလည်း Admin သီးသန့် ကန့်သတ်ခြင်း    @PreAuthorize("hasRole('ADMIN')")    @DeleteMapping("/{id}")    public ResponseEntity<Map<String, String>> deleteProduct(@PathVariable Long id) {        productService.deleteProduct(id);        return ResponseEntity.ok(Map.of("message", "Product deleted successfully"));    }}