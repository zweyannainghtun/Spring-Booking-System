package com.digitalbase.ticketing.controller;import com.digitalbase.ticketing.dto.ChangePasswordRequest;import com.digitalbase.ticketing.dto.UserCreationRequest;import com.digitalbase.ticketing.service.UserService;import lombok.RequiredArgsConstructor;import org.springframework.http.HttpStatus;import org.springframework.http.MediaType;import org.springframework.http.ResponseEntity;import org.springframework.security.access.prepost.PreAuthorize;import org.springframework.security.core.Authentication;import org.springframework.web.bind.annotation.*;import java.util.HashMap;import java.util.Map;@RestController@RequestMapping("/api/admin")@RequiredArgsConstructorpublic class AdminUserController {    private final UserService userService;    @PreAuthorize("hasRole('ADMIN')")    @PostMapping(value = "/users", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)    public ResponseEntity<Map<String, String>> createUser(@ModelAttribute UserCreationRequest request) {        String responseMessage = userService.createUserByAdmin(request);        // JSON Format ဖြင့် ပြန်လည်ပေးပို့ရန် Map ကို အသုံးပြုခြင်း        Map<String, String> response = new HashMap<>();        response.put("message", responseMessage);        return new ResponseEntity<>(response, HttpStatus.CREATED);    }    @PostMapping("/change-password")    public ResponseEntity<Map<String, String>> changePassword(            @RequestBody ChangePasswordRequest request,            Authentication authentication) {        // Login ဝင်ထားသော User ၏ Email ကို Security Context (Token) မှတစ်ဆင့် ဆွဲယူခြင်း        String email = authentication.getName();        String responseMessage = userService.changePassword(email, request);        Map<String, String> response = new HashMap<>();        response.put("message", responseMessage);        return ResponseEntity.ok(response);    }}