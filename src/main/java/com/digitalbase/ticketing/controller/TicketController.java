package com.digitalbase.ticketing.controller;import com.digitalbase.ticketing.dto.CreateTicketRequest;import com.digitalbase.ticketing.model.Ticket;import com.digitalbase.ticketing.service.TicketService;import lombok.RequiredArgsConstructor;import org.springframework.http.MediaType;import org.springframework.http.ResponseEntity;import org.springframework.web.bind.annotation.*;@RestController@RequestMapping("/api/tickets")@RequiredArgsConstructorpublic class TicketController {    private final TicketService ticketService;    @PostMapping(consumes = MediaType.MULTIPART_FORM_DATA_VALUE)    public ResponseEntity<?> createTicket(@ModelAttribute CreateTicketRequest request) {        try {            // လက်တွေ့တွင် customerId ကို SecurityContext (JWT Token) မှ ယူရပါမည်။            // ဥပမာ: Long customerId = jwtService.extractUserId(requestToken);            Long customerId = 1L; // ယာယီ (Test လုပ်ရန်)            Ticket ticket = ticketService.createTicket(request, customerId);            return ResponseEntity.ok(ticket);        } catch (Exception e) {            return ResponseEntity.badRequest().body("Error: " + e.getMessage());        }    }}