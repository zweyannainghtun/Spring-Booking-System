package com.digitalbase.ticketing.repository;import com.digitalbase.ticketing.model.TicketActivity;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;import java.util.List;@Repositorypublic interface TicketActivityRepository extends JpaRepository<TicketActivity, Long> {    // Ticket တစ်ခုချင်းစီ၏ Activity မှတ်တမ်းများကို အသစ်ဆုံးအချိန်မှစ၍ အစဉ်လိုက် ဆွဲထုတ်ရန်    List<TicketActivity> findAllByTicketIdOrderByCreatedAtDesc(Long ticketId);    // မှတ်ချက် - Dashboard ၏ Recent Activity တွင် နောက်ဆုံးလုပ်ဆောင်ချက် (၅) ခုကိုသာ ပြချင်ပါက    // Service ထဲတွင် PageRequest.of(0, 5) ကို အသုံးပြု၍ ခေါ်ယူနိုင်ပါသည်။}